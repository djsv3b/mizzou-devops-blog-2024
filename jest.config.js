module.exports = {
    testEnvironment: "node",
    testTimeout: 30000, // Optional: Increase timeout if needed for emulator connection
    setupFiles: ["<rootDir>/setupTests.js"], // Optional: If you need specific setup like environment variables
  };
  